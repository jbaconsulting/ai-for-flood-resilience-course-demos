# Make the PyTorch CPU wheels the primary index
--index-url https://download.pytorch.org/whl/cpu
# Keep PyPI as a fallback for everything else
--extra-index-url https://pypi.org/simple

pandas
matplotlib
requests

# CPU-only build; Binder will pull the CPU wheel from the PyTorch index
torch==2.4.*

sentence-transformers==2.2.2
